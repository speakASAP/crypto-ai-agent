apiVersion: v1
kind: Service
metadata:
  name: crypto-ai-agent
  namespace: statex-apps
  labels:
    app: crypto-ai-agent
    managed-by: k8s-migration
spec:
  type: ClusterIP
  selector:
    app: crypto-ai-agent
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      name: http
