FROM python:3.12-slim
WORKDIR /app
COPY requirements-ui.txt .
RUN pip install --no-cache-dir -r requirements-ui.txt
COPY app.py .
CMD ["streamlit", "run", "app.py", "--server.port=${UI_PORT:-8501}", "--server.address=${UI_HOST:-0.0.0.0}"]